// Card proportions follow the real baraja española (~57x87mm, ratio ~0.655),
// not the taller poker-card ratio (~0.71) — close enough to matter for how
// "authentic" the silhouette reads even before any artwork is drawn.
export const CARD_WIDTH = 220;
export const CARD_HEIGHT = 336;
export const CARD_VIEWBOX = `0 0 ${CARD_WIDTH} ${CARD_HEIGHT}`;
export const CARD_RADIUS = 14;
export const CARD_BORDER_INSET = 4;
